info:
  name: GetAll
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseUrl}}api/ev3/draft/{{FormCode}}/{{IToken}}/Items"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{key}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
