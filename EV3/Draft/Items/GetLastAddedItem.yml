info:
  name: GetLastAddedItem
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseUrl}}api/ev3/draft/{{FormCode}}/{{IToken}}/Items/{{LastItemRID}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{key}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
