info:
  name: Patch Last Added Item
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BaseUrl}}api/ev3/draft/{{FormCode}}/{{IToken}}/Items/{{LastItemRID}}"
  body:
    type: json
    data: '{ "Q":7}'
  auth:
    type: bearer
    token: "{{key}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
