info:
  name: Delete All Items
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BaseUrl}}api/ev3/draft/{{FormCode}}/{{IToken}}/Items"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{key}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
